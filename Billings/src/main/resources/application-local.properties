# Local Development Profile
spring.application.name=sentinel-billing
server.port=8084

# Database
spring.datasource.url=jdbc:postgresql://postgres:5432/sentinel_db
spring.datasource.username=sentinel
spring.datasource.password=sentinel123
spring.datasource.driver-class-name=org.postgresql.Driver

# Database - Connection Pool (OPTIMIZED)
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=10000

# JPA/Hibernate (OPTIMIZED)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.data.jpa.repositories.bootstrap-mode=lazy

# RabbitMQ
spring.rabbitmq.host=rabbitmq
spring.rabbitmq.port=5672
spring.rabbitmq.username=sentinel
spring.rabbitmq.password=sentinel123

# Lazy Initialization (OPTIMIZED: added)
spring.main.lazy-initialization=true

# RabbitMQ
spring.rabbitmq.host=rabbitmq
spring.rabbitmq.port=5672
spring.rabbitmq.username=sentinel
spring.rabbitmq.password=sentinel123

# JWT
jwt.secret=X9q2N8ZCnO3Tj48p1Fk6B2V0x8Teq9gHBV0SX1e2p6U=
jwt.expiration=3600000

# Microservices
services.tenant.url=http://tenant-service:8082
services.project.url=http://project-service:8083

# CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:5173,http://localhost:5174

# MercadoPago (Sandbox)
mercadopago.access_token=TEST-8162214220028576-121612-f6de21e1e57afe4ee3ca69253d301e31-1045991941
mercadopago.public_key=TEST-d92e22d6-5649-4b85-8283-54422ff420a5
mercadopago.sandbox.enabled=true
mercadopago.back_urls.success=http://localhost:3000/billing?status=success
mercadopago.back_urls.failure=http://localhost:3000/billing?status=failure
mercadopago.back_urls.pending=http://localhost:3000/billing?status=pending

# Logging
logging.level.root=INFO
logging.level.com.sentinel.billing=DEBUG

# Actuator
management.endpoints.web.exposure.include=health,info
# =============================================================================
# Performance optimizations (reduces idle CPU/RAM)
# =============================================================================
# Disable RabbitMQ health polling (Docker healthcheck covers this)
management.health.rabbit.enabled=false
# Disable OSIV (keeps JPA session open only when needed)
spring.jpa.open-in-view=false
# Disable SQL logging in console
spring.jpa.show-sql=false
# Reduce connection pool (5 per service is plenty for dev)
spring.datasource.hikari.maximum-pool-size=5
